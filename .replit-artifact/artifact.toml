kind = "web"
previewPath = "/"
title = "Creative Agency"
version = "1.0.0"
id = "artifacts/agency"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24830

[services.development]
run = "pnpm --filter @workspace/agency run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agency", "run", "build" ]
publicDir = "artifacts/agency/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24830"
BASE_PATH = "/"
